"""Admin panel: routers, texts and keyboards live in this package.

Import ``build_admin_router`` from ``app.bot.admin.router`` (kept out of this
module so that ``texts``/``keyboards`` can be imported without loading handlers).
"""
